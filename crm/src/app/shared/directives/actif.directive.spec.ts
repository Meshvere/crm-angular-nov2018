import { ActifDirective } from './actif.directive';

describe('ActifDirective', () => {
  it('should create an instance', () => {
    const directive = new ActifDirective();
    expect(directive).toBeTruthy();
  });
});
